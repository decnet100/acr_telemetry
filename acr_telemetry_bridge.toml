# acr_telemetry_bridge configuration (bridge + dashboard colors in one file)
# Paths: ./acr_telemetry_bridge.toml or ~/.config/acr_recorder/acr_telemetry_bridge.toml

rate_hz = 5
http_addr = "0.0.0.0:8080"
# udp_target = "192.168.1.255:9000"  # optional: broadcast UDP
temperature_unit = "c"
# Dashboard: which fields to show and in which order (any fields from available_fields)
dashboard_slots = [
  "water_temp", "road_temp", "air_temp", "fuel",
  "tyre_fl", "tyre_fr", "tyre_rl", "tyre_rr",
  "brake_fl", "brake_fr", "brake_rl", "brake_rr",
  "speed_kmh", "gear", "rpm"
]

# Optional: dashboard coloring (palette + per-field thresholds). Omit to use defaults or fallback to telemetry_color.toml
# [telemetry_colors.colors]
# very_low = "#1a1a5f"
# low = "#7dd3fc"
# normal = "#fde047"
# high = "#fb923c"
# very_high = "#ef4444"
# ignore = "#ffffff"
# [telemetry_colors.fields.water_temp]
# very_low = -273
# low = 0
# normal = 50
# high = 95
# very_high = 120
# (more [telemetry_colors.fields.<field_id>] as needed)
